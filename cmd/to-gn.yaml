---
# This file contains default settings. To modify any setting,
# uncomment the corresponding line and change its value.

## The name of the GN database, which defaults to 'gnames'.
#
# DbDatabase = gnames

## The host where the GN database is located.
#
# DbHost = 0.0.0.0

## The username for accessing the GN database, with write privileges.
#
# DbUser = postgres

## The password associated with the specified 'DbUser'.
#
# DbPass = postgres

## The number of concurrent jobs to execute.
#
# JobsNum = 4
